# Core API
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
pydantic-settings>=2.2
python-multipart>=0.0.9
websockets>=12.0

# Signal processing
numpy>=1.26

# Google Cloud generative backend (optional; only needed for ASSET_PROVIDER=vertex)
# Install with: pip install -r requirements-vertex.txt
